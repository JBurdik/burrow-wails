# SFTP Sync SDK starter

This is an SDK authoring example, not an installable SFTP client yet. It shows
the typed manifest a future SFTP extension will use. v1 intentionally lacks
secure credential storage, capability-scoped network access and custom native
surface primitives; those are the next host-SDK milestone.
